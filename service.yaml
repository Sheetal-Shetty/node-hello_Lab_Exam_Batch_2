apiVersion: v1
kind: Service
metadata:
  name: nodejslab1
  labels:
    app: nodejslab1
spec:
  type: NodePort
  selector:
    app: nodejslab1
  ports:
    - port: 3008        # Service port
      targetPort: 3000  # Container port (Tomcat)
      protocol: TCP
